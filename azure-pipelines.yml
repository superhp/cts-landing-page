# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'

- task: FtpUpload@2
  displayName: 'FTP Upload: $(Build.ArtifactStagingDirectory)'
  inputs:
    credentialsOption: inputs
    serverUrl: 'ftps://waws-prod-db3-087.ftp.azurewebsites.windows.net/site/wwwroot'
    username: 'cts-landing-page\$cts-landing-page'
    password: nbKPebYAcxxWwLrqNYzAld6iXXw17BS1zoQcZTdQN5NvuskXseYlvzJNCPt0
    rootDirectory: '$(Build.ArtifactStagingDirectory)'
    remoteDirectory: /
    cleanContents: true